// Code generated by wunderctl. DO NOT EDIT.

import React, { useEffect, useState } from "react";
import type { Response } from "@wundergraph/sdk";
import {
	CountryWeatherInput,
	CountryWeatherResponse,
	FakeProductsInput,
	FakeProductsResponse,
	SetPriceInput,
	SetPriceResponse,
} from "./models";
import { useQuery, useLiveQuery, useMutation } from "./hooks";
import jsonSchema from "./jsonschema";
import Form from "@rjsf/core";

export interface FormProps<T> {
	onResult?: (result: T) => void;
	liveValidate?: boolean;
}

export interface MutationFormProps<T> extends FormProps<T> {
	refetchMountedQueriesOnSuccess?: boolean;
}

export const SetPriceForm: React.FC<MutationFormProps<Response<SetPriceResponse>>> = ({
	onResult,
	refetchMountedQueriesOnSuccess,
	liveValidate,
}) => {
	const [formData, setFormData] = useState<SetPriceInput>();
	const { mutate, response } = useMutation.SetPrice({ refetchMountedQueriesOnSuccess });
	useEffect(() => {
		if (onResult) {
			onResult(response);
		}
	}, [response]);
	return (
		<div>
			<Form
				schema={jsonSchema.SetPrice.input}
				formData={formData}
				liveValidate={liveValidate}
				onChange={(e) => {
					setFormData(e.formData);
				}}
				onSubmit={async (e) => {
					await mutate({ input: e.formData, refetchMountedQueriesOnSuccess });
					setFormData(undefined);
				}}
			/>
		</div>
	);
};

export const CountryWeatherForm: React.FC<FormProps<Response<CountryWeatherResponse>>> = ({
	onResult,
	liveValidate,
}) => {
	const [formData, setFormData] = useState<CountryWeatherInput>();
	const { response, refetch } = useQuery.CountryWeather({ input: formData });
	useEffect(() => {
		if (onResult) {
			onResult(response);
		}
	}, [response]);
	return (
		<div>
			<Form
				schema={jsonSchema.CountryWeather.input}
				formData={formData}
				liveValidate={liveValidate}
				onChange={(e) => {
					setFormData(e.formData);
				}}
				onSubmit={async (e) => {
					await refetch({ input: formData });
				}}
			/>
		</div>
	);
};
export const FakeProductsForm: React.FC<FormProps<Response<FakeProductsResponse>>> = ({ onResult, liveValidate }) => {
	const [formData, setFormData] = useState<FakeProductsInput>();
	const { response, refetch } = useQuery.FakeProducts({ input: formData });
	useEffect(() => {
		if (onResult) {
			onResult(response);
		}
	}, [response]);
	return (
		<div>
			<Form
				schema={jsonSchema.FakeProducts.input}
				formData={formData}
				liveValidate={liveValidate}
				onChange={(e) => {
					setFormData(e.formData);
				}}
				onSubmit={async (e) => {
					await refetch({ input: formData });
				}}
			/>
		</div>
	);
};

export const CountryWeatherLiveForm: React.FC<FormProps<Response<CountryWeatherResponse>>> = ({
	onResult,
	liveValidate,
}) => {
	const [formData, setFormData] = useState<CountryWeatherInput>();
	const { response } = useLiveQuery.CountryWeather({ input: formData });
	useEffect(() => {
		if (onResult) {
			onResult(response);
		}
	}, [response]);
	return (
		<div>
			<Form
				schema={jsonSchema.CountryWeather.input}
				formData={formData}
				liveValidate={liveValidate}
				onChange={(e) => {
					setFormData(e.formData);
				}}
				children={<></>}
			/>
		</div>
	);
};
export const FakeProductsLiveForm: React.FC<FormProps<Response<FakeProductsResponse>>> = ({
	onResult,
	liveValidate,
}) => {
	const [formData, setFormData] = useState<FakeProductsInput>();
	const { response } = useLiveQuery.FakeProducts({ input: formData });
	useEffect(() => {
		if (onResult) {
			onResult(response);
		}
	}, [response]);
	return (
		<div>
			<Form
				schema={jsonSchema.FakeProducts.input}
				formData={formData}
				liveValidate={liveValidate}
				onChange={(e) => {
					setFormData(e.formData);
				}}
				children={<></>}
			/>
		</div>
	);
};
