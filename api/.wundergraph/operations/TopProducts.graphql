query ($random: Boolean){
    topProducts: federated_topProducts(random: $random) {
        upc
        name
        price
        reviews {
            id
            body
            author {
                id
                name
                username
                reviews {
                    product {
                        name
                    }
                }
            }
        }
    }
}